import CalcBoton from '../calcBoton/CalcBoton'
import { tiposBotones } from '../../helpers/tiposBotones'

import './calcFooter.css'

const CalcFooter = () => {
    return(
        <div className="calcFooter-contenedor">
            {
                // tiposBotones.map(filaBtn => {


                //     return (
                //         <button></button>
                //     )
                // })
            }
        </div>
    )
}

export default CalcFooter